# dependencies
/node_modules
/.pnp
.pnp.*

# next.js build output (dev writes .next-dev; every build uses .next — see next.config.ts)
/.next-dev
/.next
/out

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
.env*

# typescript
*.tsbuildinfo
next-env.d.ts

# scenario scratch — raw drops live here, not in the tree
_inbox/
.vercel
